// import {ADD_VIDEOGAME} from '../actions/index'
import { ADD_VIDEOGAME } from "../actions";

const initialState = [];

const rootReducer = (state = initialState, action) => {
    switch(action.type) {
      // Aca va tu codigo;  
        
        default:
        return state;
    }
  }
  
  export default rootReducer;